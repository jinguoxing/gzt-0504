/**
 * 路由路径映射
 * 对齐 DataSemanticHub 的 MODULE_PATH_MAP 风格
 */
export const ROUTE_PATH_MAP: Record<string, string> = {
  home: '/',
  draft: '/draft',
  execution: '/execution',
  data_query: '/data-query',
  task_list: '/task-list',
};
